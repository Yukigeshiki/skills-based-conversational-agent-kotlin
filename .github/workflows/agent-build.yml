name: Agent Build

on:
  push:
    branches: [main]
    paths:
      - 'services/agent/**'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docker build
        run: docker build -t agent:${{ github.sha }} services/agent
