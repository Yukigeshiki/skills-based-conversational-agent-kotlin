name: UI Build

on:
  push:
    branches: [main]
    paths:
      - 'ui/**'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docker build
        run: docker build -t ui:${{ github.sha }} ui
